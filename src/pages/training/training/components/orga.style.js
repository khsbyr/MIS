import styled from 'styled-components';

const OrgaStyle = styled.div`
  Select {
    text-align: center;
  }

  .ant-select-single:not(.ant-select-customize-input) .ant-select-selector {
    height: 33px;
    padding-top: 6px;
    border: none;
  }
`;

export default OrgaStyle;
