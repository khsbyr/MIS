import styled from 'styled-components';

const ContentWrapper = styled.div`
  .header {
    margin-right: 15px;
    margin-left: 15px;
  }

  .ant-table {
    font-size: 11px;
  }
`;

export default ContentWrapper;
