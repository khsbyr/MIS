import styled from 'styled-components'

const HeaderWrapper = styled.div`
    margin-top: 30px;

    .title {
        font-size: 24px;
        font-weight: Bold;
    }

    .ant-btn {
        border: none;
        font-size: 15px;
    }

`;

export default HeaderWrapper;